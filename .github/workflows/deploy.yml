name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

# Permissions required by the official Pages deployment action.
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the
# run in-progress and latest queued.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install

      - name: Build static site
        # The client/ssr builds and prerender all complete and write the
        # static site to .output/public before Nitro's (unneeded for a static
        # site) production server build fails — so don't let that fail the job.
        run: npm run build || echo "::warning::build exited non-zero (likely the unused Nitro server step); continuing"
        env:
          # Nitro preset that emits a static site (.output/public) with
          # .nojekyll and a 404.html fallback, ready for GitHub Pages.
          NITRO_PRESET: github_pages

      - name: Prepare static output for Pages
        run: |
          cd .output/public
          echo "=== output tree ==="; find . -type f -exec ls -l {} \; | sort -k9
          # SPA mode emits the client-rendered shell; normalize whatever it is
          # called (index, _shell.html, ...) into index.html for GitHub Pages.
          if [ ! -f index.html ]; then
            if [ -f index ]; then mv index index.html;
            elif [ -f _shell.html ]; then cp _shell.html index.html; fi
          fi
          # Fail loudly (with a non-empty check) if no usable shell was produced.
          test -s index.html
          # SPA-style fallback so deep links / refreshes don't 404.
          cp index.html 404.html
          # Tell GitHub Pages not to run the output through Jekyll.
          touch .nojekyll
          echo "=== index.html (first 1500 chars) ==="; head -c 1500 index.html

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .output/public

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
